<div class="row">
	<div class="col-sm-2">
		<%= render "layouts/navigation"%>
	</div>

	<div class="container show-posts-comments">
		<%= @comment.body %>
		<%= link_to "Delete", post_comment_path(@post.id, @comment.id), :method => :delete, :confirm => "Are you sure?" %>
	</div>

</div>