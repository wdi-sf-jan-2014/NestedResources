<h1>Add a Comment</h1>

<%= form_for [@parent, @comment] do |f| %>
  <p>
    <%= f.label :link %><br>
    <%= f.text_field :body %>
  </p>
  
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  
  <p><%= f.submit 'add comment' %></p>
<% end %>