<%= (@comment.comment.body if @comment.comment) ||  (@comment.post.body if @comment.post) %></br>
<%= @comment.body || "New comment"%>
<%= form_for [(@comment.comment || @comment.post), @comment ] do |f| %>
    <%= f.label "Comment" %>
    <%= f.text_field :body %>
    <%= f.submit %>
<% end %>