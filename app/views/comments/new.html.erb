<%= form_for @comment do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body %> 
  <%= f.fields_for :comments, @comment.comments.build do |c| %>
    <%= c.label :body, 'Comment text' %>
    <%= c.text_field :body %>
  <% end %>
  <%= f.submit "Add" %>
<% end %>
