<%= @comment.body %>
<%= form_for @comment do |f| %>
  <%= f.fields_for :comments, @comment.comments.build do |c| %><br>
    <%= c.label :body, 'Comment text' %>
    <%= c.text_field :body %>
 <% end %><br>
  <%= f.submit "Add" %>
<% end %>
