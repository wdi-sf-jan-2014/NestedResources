<h1>List of Posts</h1>

<% @posts.each do |p| %>
	<h2><%= link_to p.body, p.link %></h2>
	<div>(Link: <%= p.link %>)</div>
	<div><%= link_to "#{p.comments.count} Comments", "/posts/#{p.id}" %></div>
<% end %>

<%= button_to "New Post", new_post_path, method: :get %>
